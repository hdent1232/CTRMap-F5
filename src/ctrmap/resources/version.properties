# The running build's version. Bump this, commit, then publish a GitHub release
# whose tag matches (package.ps1 does both from one number).
# Older copies compare their version against the latest release's tag, so this
# number is the only thing that decides whether a user is offered an update.
version=1.0.0
