{
 "_why": "Every rule in CLAUDE.md and the mechanism that refuses it, checked against the tree by tools/guard/rule_map.py. An entry naming nothing real is refused; a rule with no entry at all is refused; the number with no mechanism may only fall. On 2026-09-20 sixteen of these twenty-eight had nothing behind them, which nobody could see because nobody had counted.",
 "unenforced_ceiling": 0,
 "rules": {
  "MEASURE, NEVER REASON FROM PLAUSIBILITY": {
   "by": "commit_guard.check_zero_claim",
   "note": "a measurement reporting nothing must cite the control that proves the scan can find something; census.py refuses a report whose coverage is not computed from disk",
   "clauses": {
    "Any scan you write must include a known-answer control before its result is believed": "commit_guard.check_zero_claim"
   },
   "clause_notes": {
    "Any scan you write must include a known-answer control before its result is believed": "the point at which a scan's result is BELIEVED is a commit that reports it, and a message reporting nothing found is refused unless it names the control that proves the scan can find something; magnitude.py refuses the other direction, a number that moved past its allowance or did not move across a change meant to move it. Both were paid for: a scan read 0 across 536 files twice for reading the wrong struct field, and a ratchet re-measured to exactly 103 before and after when the truth was 64"
   }
  },
  "A FIX WITHOUT A GUARD IS NOT A FIX": {
   "by": "commit_guard.check_fix_has_a_guard",
   "note": "a commit whose diff changes behaviour must touch a guard directory"
  },
  "PROVE EVERY GUARD BY BREAKING IT": {
   "by": "tools/guard/replant.py",
   "note": "175 recorded defects, each re-applied and required to redden its named suite"
  },
  "A CONFIDENT EMPTY RESULT IS A BUG UNTIL PROVEN OTHERWISE": {
   "by": "commit_guard.check_zero_claim",
   "note": "and census.py refusal 1: a census that read nothing is refused rather than reported clean"
  },
  "COUNT WHAT YOU EXPECT BEFORE RUNNING THE CHANGE": {
   "by": "tools/guard/count_first.py",
   "note": "a rewrite that reaches a file without counting its matches first is refused; it found autoplant.try_one writing a substitution straight out while replant.py, the same function one directory over, had refused an ambiguous find since it was written"
  },
  "A STARTLING MAGNITUDE IS A BUG REPORT": {
   "by": "tools/guard/magnitude.py",
   "note": "a recorded number that moved past its allowance, or did NOT move across a change meant to move it, is refused without a reason; wired into record_run.py and replant.py --owed. The second case was paid for on 2026-09-20: a ratchet re-measured to exactly 103 before and after, and the true number was 64"
  },
  "BUILD GENERAL SYSTEMS, NOT THE USER'S EXAMPLE": {
   "by": "tools/guard/game_identity.py",
   "note": "a game's identity hardcoded in production code outside src/ctrmap/gamedef is refused; two known sites (ZoneLimitPatch, ZoneLoadingPanel) are named and ratcheted, and a third is refused. Fixing the two is a behaviour change the owner has to check in the game, so it is not slipped into a guard commit"
  },
  "DETANGLE, DO NOT RELOCATE": {
   "by": "commit_guard.check_detangle",
   "note": "a commit that says it split something and adds production files must show its coupling going down"
  },
  "EXTRACTION IS NOT TESTING": {
   "by": "commit_guard.check_extraction_is_tested",
   "note": "a new production class no test names is refused"
  },
  "NEVER HAND-EDIT A MEASUREMENT": {
   "by": "src/ctrmap/tests/MutationBaselineTest.java",
   "note": "the baseline's digest is recomputed from the tree, so an edited number is refused"
  },
  "COMMIT BEFORE RUNNING ANYTHING THAT MUTATES SOURCES": {
   "by": "work_order.dirty_files",
   "note": "a frozen-tree measurement refuses to begin on a dirty tree"
  },
  "NEVER EDIT SOURCE WHILE THE SUITE IS RUNNING": {
   "by": "tools/hooks/guard_suite_running.py",
   "note": "a write to anything the build compiles is refused while a battery or mutation run holds its lock - and since 2026-09-21 record_run digests the tree on BOTH sides of the battery and refuses to record a run whose subject moved underneath it, which is the half that does not care whether the edit went through a tool or through the owner's IDE"
  },
  "RECORD TRAPS YOU PAID FOR, WITH THE COST": {
   "by": "replant.without_a_cost",
   "note": "a falling ceiling on plants whose why records no number"
  },
  "NOTHING IS FIXED UNTIL THE SECOND ONE IS IMPOSSIBLE, AND THE RULE IS THE AUTHORISATION": {
   "by": "commit_guard.check_guard_class",
   "note": "a detector or a convention is refused without a No-refusal: line saying why the point of action cannot refuse"
  },
  "ASK WHAT THE CHEAPEST WAY TO SATISFY YOUR OWN GUARD IS, AND REFUSE THAT TOO": {
   "by": "commit_guard.check_cheapest_evasion",
   "note": "a commit adding a guard must write down the cheapest way to make that guard go quiet"
  },
  "AN UNMEASURABLE QUANTITY IS NOT A SMALL ONE": {
   "by": "guard_fanout.script_width",
   "note": "a fan-out whose width cannot be read is refused rather than assumed small; shellin.unreadable does the same for a tool input"
  },
  "A QUERY THAT CANNOT READ ITS SUBJECT MUST NOT REPORT IT ABSENT": {
   "by": "tools/guard/liveness_check.py",
   "note": "one probe in tools/hooks/liveness.py that returns its reason, and a refusal for any other spelling in any .py or .ps1 here"
  },
  "A REMEDY'S EXIT CODE ANSWERS ITS OWN QUESTION, NOT YOURS": {
   "by": "tools/hooks/guard_exit_code.py",
   "note": "reading $? after a pipeline into a filter is refused; ${PIPESTATUS[0]} is not"
  },
  "A RUN THAT KNOWS WHAT IT MISSED GOES BACK": {
   "by": "commit_guard.check_known_hole",
   "note": "and census.py, which refuses a report whose scope was not covered - stopping the search fails one, filing the result fails the other",
   "clauses": {
    "Never report a gap and stop": "commit_guard.check_known_hole",
    "Never end by naming what you did not cover and handing it over": "commit_guard.check_known_hole"
   },
   "clause_notes": {
    "Never end by naming what you did not cover and handing it over": "section 1's wording of the clause section 2 states as 'Never report a gap and stop' - one rule written twice in one file, and until 2026-09-21 a dict keyed by headline kept one of the two bodies, so this half was never audited. Same refusal answers it: a commit naming a hole it did not close must say why the run could not close it"
   }
  },
  "A CLAIM ABOUT WHAT YOU DID NOT OPEN IS A MEASUREMENT": {
   "by": "commit_guard.check_blanket_claim",
   "note": "vouching for what the diff did not open needs Reviewed: n/n by a path that exists"
  },
  "DO THE WORK MECHANICALLY; AN AGENT FAN-OUT NEEDS A YES AND A PRICE": {
   "by": "tools/hooks/guard_fanout.py",
   "note": "caps agents, workflows and fan-out width, and refuses a width it cannot read"
  },
  "DO THE WORK MECHANICALLY. AGENT SWEEPS ARE THE LAST RESORT": {
   "by": "tools/hooks/guard_fanout.py",
   "note": "the owner's own wording of the same rule; the same hook answers both - the ledger carried this rule under HALF its name until 2026-09-21, because the parser cut a headline at its first full stop while the body parser did not, and the clause audit then looked the body up by a name that did not exist and found none"
  },
  "MAKE IT IMPOSSIBLE AT THE POINT OF ACTION": {
   "by": "commit_guard.check_guard_class",
   "note": "the kind of guard a fix carries must be named, and a detector must say why a refusal was impossible"
  },
  "NEVER PUSH WITHOUT EXPLICIT PER-PUSH APPROVAL": {
   "by": "tools/hooks/guard_push.py",
   "note": "a push needs a token naming THIS commit; one more commit expires it"
  },
  "FINISH CHANGES BEFORE STARTING A LONG MEASUREMENT": {
   "by": "work_order.blockers",
   "note": "OUTSTANDING.md and the plant-proof digest both gate a frozen-tree measurement"
  },
  "FEATURES LIVE IN THEIR OWN UI AREA": {
   "by": "commit_guard.check_menu_item",
   "note": "a new item on the window that owns the menu bar must say why it is not a tab; MainframeShapeTest caps how many the bar may hold",
   "clauses": {
    "Never menu-dumped, never a new window": "src/ctrmap/tests/DialogSeamTest.java",
    "Undo/redo everywhere": "commit_guard.check_undo"
   }
  },
  "GAME DATA IS READ-ONLY": {
   "by": "tools/hooks/guard_game_data.py",
   "note": "a write under a protected root is refused from any tool; reads are untouched",
   "clauses": {
    "Never run Tidewater": "tools/hooks/guard_forbidden_command.py",
    "Never `git stash": "tools/hooks/guard_forbidden_command.py",
    "Never hand-edit a NetBeans `initComponents` block": "tools/hooks/guard_generated_code.py",
    "No Bash heredocs for Java or Python — write the script to a file": "tools/hooks/guard_heredoc.py"
   }
  },
  "THE OWNER DOES THE IN-APP AND IN-GAME TESTING": {
   "by": "commit_guard.check_in_game_claim",
   "note": "a commit claiming an in-game result is refused unless it quotes the owner"
  },
  "AGENTS THAT CANNOT EDIT EACH OTHER'S FILES MUST NOT EDIT AT THE SAME TIME": {
   "by": "tools/hooks/guard_fanout.py",
   "note": "parallel_edit reads the fan-out's own script text and refuses a launch whose workers WRITE in parallel, however many they are - a survey composes and a parallel rewrite does not, which is why 476 finding headlines over 41 locations closed nothing. The rule was running here before it was written down, which is as invisible as the opposite"
  },
  "A GUARD'S TRIGGER IS A CLASS, NOT A LIST OF NAMES": {
   "by": "tools/guard/hooks_wired.py",
   "note": "hooks_that_decide_by_tool_name refuses a hook that opens by comparing tool_name to a literal, and the wiring check refuses any matcher that is a tool name rather than \"*\" - 8 hooks here were wired under Bash in a session that also offered PowerShell, installed and running and off for every command issued through it"
  },
  "A RECORDED MEASUREMENT MUST CARRY THE SUBJECT IT WAS TAKEN AGAINST": {
   "by": "tools/guard/commit_guard.py",
   "note": "record_is_about_another_tree refuses a count whose record carries no subject digest, or one that does not match this tree, and record_run refuses to write a record at all for a run whose tree moved while it ran; source_digest treats a subject it cannot compute as UNKNOWN rather than unchanged",
   "clauses": {
    "The record must not be part of its own subject — exclude it by SHAPE, because excluding it by ignore-rule holds in your repository": "commit_guard.source_digest"
   },
   "clause_notes": {
    "The record must not be part of its own subject — exclude it by SHAPE, because excluding it by ignore-rule holds in your repository": "the subject digest covers everything git calls source MINUS dotfiles at the repository root, which is where the records live - a shape, not a list of names, because excluding the record by ignore-rule holds in this repository and fails in the scratch repositories the suites build, which carry no .gitignore at all. Proven by the plant test_a_record_at_the_ROOT_is_not_part_of_its_OWN_subject in the bootstrap bundle and by the control that drives it here"
   }
  }
 }
}
